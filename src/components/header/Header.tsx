import styles from "./header.module.css";

export const Header = () => {
  return (
    <header className={styles.headerClass}>
      <h1 className={styles.mainText}>cards-react app</h1>
    </header>
  );
};
